/**
 * Analog domain — public API (stub).
 *
 * Read models for the historical analog engine (analog_query / analog_match). The
 * search compute lives in workers/tickertea/analog/. Powers POST /api/v1/analogs/search.
 * See docs/architecture/05-historical-analog-engine.md.
 */
export {};

/**
 * Company domain — public API (stub).
 *
 * Owns the company master, external identifiers, index membership, and the
 * `in_universe` flag. Read path for the app; write path is the ingestion workers.
 * Follow the signal domain's repository/service/index shape when implementing.
 */
export {};

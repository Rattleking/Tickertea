/**
 * Evidence domain — public API (stub).
 *
 * Serves the traceability chain for a signal: signal_evidence -> ingest_event -> raw_uri.
 * Powers GET /api/v1/signals/{id}/evidence. Follow the signal domain shape.
 */
export {};
